import React from "react";
import { ReturnButton } from "./settings.styles";
import { View } from 'react-native';

export const SettingsScreen = () => {
    return (
        <View>
            <ReturnButton></ReturnButton>
        </View>
    );
}