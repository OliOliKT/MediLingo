import React from "react";
import { Text, View } from 'react-native';

export const ConversationHistoryScreen = () => {
    return (
        <View>
            <Text>Conversation History Screen</Text>
        </View>
    );
}